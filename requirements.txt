# Minimaal nodig om de app te draaien
flask>=3.0

# productie-server voor hosting (mini pc / thuisnetwerk)
waitress>=3.0

# Garmin Connect-sync (unofficial client)
garminconnect>=0.3.11

# EXIF-loze herschaling van maaltijdfoto's vóór cloudanalyse
pillow>=10.0